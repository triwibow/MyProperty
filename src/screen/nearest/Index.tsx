import Container from "../../components/container/Container";
import Nearest from "../../container/nearest/Nearest";

const Index = (props:any) => {
	return (
		<Container>
			<Nearest {...props} />
		</Container>
	)
}

export default Index;